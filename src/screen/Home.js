import { View, Text, StyleSheet } from "react-native";
import React from "react";
import Container from "../components/container";

const Home = () => {
  return (
    <Container>
      <Text style={{ fontFamily: "Bold", color: "white" }}>Home</Text>
    </Container>
  );
};

const styles = StyleSheet.create({});

export default Home;
